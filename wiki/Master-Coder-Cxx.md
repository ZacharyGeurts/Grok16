# Master Coder — C++

Grok16 @ **16.2.0** — every C++ command for the unified `g16` driver.

**Web:** [master-coder-cxx.html](https://zacharygeurts.github.io/Grok16/master-coder-cxx.html)

Use `g16` or `g++16` (symlink). Detection: `.cpp`, `-std=gnu++26`, `g++16` basename. Backend: `libexec/grok16/g16-cxx`.

See the web index for profiles, benches, PGO, `g16_field_mandate`, World_Redata gates.