# Master Coder — C

Grok16 @ **16.2.0** — every C command for the unified `g16` driver.

**Web:** [master-coder-c.html](https://zacharygeurts.github.io/Grok16/master-coder-c.html)

Use `g16` only (no separate C binary). Detection: `.c`, `-std=gnu17`, `G16_FORCE_C`. Backend: `libexec/grok16/g16-cc`.

See the web index for full detail columns: PROFILE `c` / `c_link` / `c_pgo_*`, `G16_C_STD`, verify C leg, paths, bootstrap.